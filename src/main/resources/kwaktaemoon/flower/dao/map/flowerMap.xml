<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='kwaktaemoon.flower.dao.map.FlowerMap'>
	<select id='selectFlowers' resultMap='flowerMap'>
		select flower_num, flower_name, price, amount, kind, flower_category,
		flower_size, flower_imgfile_name, detail_imgfile_name, red_date
		from flowers
		order by flower_num desc
	</select>	
	
	
	<select id='searchFlowerWithKeyword' resultMap='flowerMap'>
		select * from flowers
		WHERE flower_name LIKE '%'||#{keyword}||'%'
	</select>
	
	<select id='searchFlower' resultMap='flowerMap'>
		select * from flowers
		where flower_num = #{flowerNum}
	</select>	
	
	<insert id='insertFlowerAdmin'>
		insert into flowers(flower_num, flower_name, price, amount, kind, flower_category,
				flower_size, flower_imgfile_name, detail_imgfile_name, reg_date)
		values(#{flowerNum}, #{flowerName}, #{price}, #{amount}, #{flowerCategory}, 
				#{flowerSize}, #{flowerImgfileName}, #{detailImgfileName}, #{regDate})	
	</insert>	
	
	<update id='updateFlowerAdmin'>
		update flowers
		set 
	</update>
	
</mapper>
